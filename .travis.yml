sudo: required
dist: trusty

# addons:
#   apt:
#     packages:
#       - thrift-compiler

before_install:

before_script:
  - PYTHONPATH=/Users/arcarter/code/modeldb/client/python:$PYTHONPATH
  - pip install -r $TRAVIS_BUILD_DIR/client/python/requirements.txt
  - pwd
  - cd $TRAVIS_BUILD_DIR/server
  - ./start_server.sh 0.9.0-3 &
  - cd $TRAVIS_BUILD_DIR/frontend
  - ./start_frontend.sh &

script:
  -

